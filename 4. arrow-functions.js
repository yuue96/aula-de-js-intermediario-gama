// Arrow Functions (funções anônimas ou callback)

const soma = (num1, num2) => num1 + num2


const sayHello = (name) => `Hello ${name}`

console.log(sayHello("Mari"))

// Ao invés de:
// function soma(a,b) { 
//    return a + b
// }

// console.log(soma(3,5))